import { Center, Text } from "@mantine/core";

const NoResultsFound = () => (
    <Center mt={200}>
        <Text fs="italic" fw={500} size='xl'>No Search Results Found</Text>
    </Center>
)

export default NoResultsFound